import { layoutStyle } from '@/styles/layer.css';

export const start = layoutStyle({ alignItems: 'flex-start' });

export const center = layoutStyle({ alignItems: 'center' });

export const end = layoutStyle({ alignItems: 'flex-end' });

export const stretch = layoutStyle({ alignItems: 'stretch' });
